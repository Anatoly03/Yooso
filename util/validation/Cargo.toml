[package]
name = "util-validation"
version = "0.1.0"
edition = "2024"

[features]
default = ["uuid", "util"]
uuid = ["dep:uuid"]
util = ["dep:regex"]

[dependencies]
regex = { version = "1.0", optional = true }
uuid = { version = "1.23.1", features = [ "v7", "serde" ], optional = true }
