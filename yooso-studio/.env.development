# The API/ backend endpoint. For development it is usually the local port 8000.
# Default rocket port.
VITE_API_SERVER=http://localhost:8000

# Wether the app is built as a demo. In this mode the client will be wrapped with
# a demonstrative component that shows a simple browser window. For development and
# production it is set to false.
VITE_DEMO_MODE=false